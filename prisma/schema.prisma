generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

model Button {
  id  Int  @id @default(autoincrement())
  categoria  String
  tamano String
  estilo String
  htmlCode String
  cssCode String
  tailwindCode String
}
